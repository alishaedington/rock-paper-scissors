if (playerScore <= 5 || computerScore <= 5);

/*when I use this, the program will continue until both players have a score of OVER 5 and 
then it will display a message*/

function isEmpty(str){
    if (str == '') {
        return true;
    } else {
        return false;
    }
}